<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/screen_background"
    android:orientation="vertical">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        style="@style/Widget.Kumbaram.Toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:title="@string/tab_rewards" />

    <com.google.android.material.button.MaterialButtonToggleGroup
        android:id="@+id/contentToggle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="@dimen/space_md"
        android:layout_marginTop="@dimen/space_sm"
        app:checkedButton="@id/rewardsToggleButton"
        app:selectionRequired="true"
        app:singleSelection="true">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/rewardsToggleButton"
            style="@style/Widget.Kumbaram.Button.Outline"
            android:layout_width="0dp"
            android:layout_height="@dimen/compact_button_height"
            android:layout_weight="1"
            android:text="@string/admin_panel_segment_rewards" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/notificationsToggleButton"
            style="@style/Widget.Kumbaram.Button.Outline"
            android:layout_width="0dp"
            android:layout_height="@dimen/compact_button_height"
            android:layout_weight="1"
            android:text="@string/admin_panel_segment_notifications" />
    </com.google.android.material.button.MaterialButtonToggleGroup>

    <murat.com.saasproject.ui.common.StateLayout
        android:id="@+id/stateLayout"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
            android:id="@+id/swipeRefresh"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recyclerView"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"
                android:paddingTop="@dimen/space_sm"
                android:paddingBottom="@dimen/space_lg" />
        </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>
    </murat.com.saasproject.ui.common.StateLayout>
</LinearLayout>
